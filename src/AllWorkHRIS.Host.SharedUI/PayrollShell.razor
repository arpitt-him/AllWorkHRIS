@inherits LayoutComponentBase
@layout MainLayout
@inject NavigationManager Nav
@using Microsoft.AspNetCore.Components.Sections

@* Tab strip renders into MainLayout's fixed slot (above .shell-content), so it stays
   pinned while the page scrolls at the window edge. *@
<SectionContent SectionName="shell-tabs">
    <div class="payroll-shell__tabs">
        <AuthorizeView Roles="PayrollOperator,PayrollAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/runs")" href="/payroll/runs">Payroll Runs</a>
        </AuthorizeView>
        <AuthorizeView Roles="PayrollOperator,PayrollAdmin,HrisAdmin,BenefitsAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/register")" href="/payroll/register">Pay Register</a>
        </AuthorizeView>
        <AuthorizeView Roles="PayrollOperator,PayrollAdmin,HrisAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/accumulators")" href="/payroll/accumulators">Accumulators</a>
        </AuthorizeView>
        <AuthorizeView Roles="PayrollAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/calendar")" href="/payroll/calendar">Pay Calendars</a>
        </AuthorizeView>
        <AuthorizeView Roles="PayrollAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/profiles")" href="/payroll/profiles">Payroll Profiles</a>
        </AuthorizeView>
        <AuthorizeView Roles="PayrollAdmin,HrisAdmin" Context="_">
            <a class="payroll-tab @ActiveClass("/payroll/year-end")" href="/payroll/year-end">Year-End</a>
        </AuthorizeView>
    </div>
</SectionContent>

@Body

@code {
    private string ActiveClass(string segment)
    {
        var uri = Nav.ToBaseRelativePath(Nav.Uri);
        return uri.StartsWith(segment.TrimStart('/'), StringComparison.OrdinalIgnoreCase)
            ? "payroll-tab--active"
            : "";
    }
}
