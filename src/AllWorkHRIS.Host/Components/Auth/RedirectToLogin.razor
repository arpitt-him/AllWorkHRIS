@* AllWorkHRIS.Host/Components/Auth/RedirectToLogin.razor *@
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/account/login", forceLoad: true);
    }
}
